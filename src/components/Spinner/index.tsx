import './style.css';

import { CircularProgress } from '@material-ui/core';

const Spinner = () => (
    <div className='spinner'>
        <CircularProgress />
    </div>
);

export default Spinner;
